''' idseq_dag '''
# Vendored into seqtoid-workflows; version is managed by the parent repo. This
# MUST be a valid PEP 440 string — modern setuptools rejects arbitrary text
# (the previous "EXTERNALLY_MANAGED" broke editable installs and will break
# image builds once the workflow base images modernize). See CZID-347.
__version__ = "0.0.0"
